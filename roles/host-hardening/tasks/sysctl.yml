---
# Network/kernel hardening sysctls. Idempotent.

- name: Render sysctl drop-in
  ansible.builtin.copy:
    dest: /etc/sysctl.d/99-host-hardening.conf
    owner: root
    group: root
    mode: "0644"
    content: |
      # Managed by ansible — see roles/host-hardening/tasks/sysctl.yml.

      # Network — anti-spoofing + ICMP redirects
      # rp_filter=2 (loose) is required on this cluster — strict mode (1)
      # broke libvirt+WireGuard asymmetric routing (see
      # docs/security-audit-2026-04-30.md → mars-firewall-incident.md).
      net.ipv4.conf.all.rp_filter = 2
      net.ipv4.conf.default.rp_filter = 2
      net.ipv4.conf.all.accept_source_route = 0
      net.ipv4.conf.default.accept_source_route = 0
      net.ipv4.conf.all.accept_redirects = 0
      net.ipv4.conf.default.accept_redirects = 0
      net.ipv4.conf.all.secure_redirects = 0
      net.ipv4.conf.default.secure_redirects = 0
      net.ipv4.conf.all.send_redirects = 0
      net.ipv4.conf.default.send_redirects = 0
      net.ipv4.conf.all.log_martians = 1
      net.ipv4.icmp_echo_ignore_broadcasts = 1
      net.ipv4.icmp_ignore_bogus_error_responses = 1
      net.ipv4.tcp_syncookies = 1

      # IPv6 — same anti-spoofing
      net.ipv6.conf.all.accept_source_route = 0
      net.ipv6.conf.default.accept_source_route = 0
      net.ipv6.conf.all.accept_redirects = 0
      net.ipv6.conf.default.accept_redirects = 0

      # Kernel — restrict info leaks
      kernel.kptr_restrict = 2
      kernel.dmesg_restrict = 1
      kernel.unprivileged_bpf_disabled = 1
      kernel.yama.ptrace_scope = 1
  notify: Reload sysctl
