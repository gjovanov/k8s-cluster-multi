---
# Phase 7: K8s prerequisites on all 6 VMs
# Containerd, kubeadm, kubelet, kubectl, Helm, /etc/hosts
- name: "Phase 7: K8s prerequisites"
  hosts: k8s_cluster
  become: true
  roles:
    - k8s-common
